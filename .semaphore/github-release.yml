version: v1.0
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: Release to GitHub
    task:
      secrets:
        - name: gh-token
      jobs:
        - name: Create Release
          commands:
            - checkout
            - bash .semaphore/release